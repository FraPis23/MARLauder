# Core GPU stack. torch is already provided by the Docker base image
# (pytorch/pytorch:2.7.1-cuda12.8-cudnn9-runtime); pip sees the requirement as satisfied there.
# Warp compiles its kernels at runtime for whichever GPU is present, so one image serves
# Ada (sm_89) and Blackwell (sm_120) alike.
torch==2.7.1
warp-lang==1.15.0

# Map I/O and host-side preprocessing
numpy>=1.26,<3
Pillow>=10.0
imageio>=2.31

# Utilities
tqdm>=4.66

# Optional — experiment tracking. Logging is OFF unless --wandb is passed; every metric is
# written to runs/<run>/metrics.jsonl regardless.
wandb>=0.16
