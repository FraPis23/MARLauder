# Core GPU stack (torch provided by base image cu128; warp compiles kernels
# at runtime for the present GPU -> same image works on 4080 and 5080).
torch>=2.7
warp-lang>=1.7

# Map I/O & host-side prep
numpy
Pillow
imageio

# Utils
tqdm

# Experiment tracking + hyperparameter sweeps
wandb
